{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "Stop": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "UNTRACKED=$(git status --porcelain 2>/dev/null); if [ -n \"$UNTRACKED\" ]; then echo '--- git status (uncommitted changes present) ---'; echo \"$UNTRACKED\"; echo '--- Consider committing or explicitly discarding these before ending the session. ---'; fi"
          }
        ]
      }
    ]
  },
  "permissions": {
    "allow": [
      "Bash(git status:*)",
      "Bash(git diff:*)",
      "Bash(git log:*)",
      "Bash(git fetch:*)",
      "Bash(git branch:*)",
      "Bash(npm run build)",
      "Bash(npm ci)",
      "Bash(npx tsc --noEmit)",
      "Bash(npx tsc --noEmit:*)"
    ]
  }
}
